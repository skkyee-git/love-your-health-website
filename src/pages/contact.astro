---
import BaseLayout from '../layouts/BaseLayout.astro';
---

<BaseLayout title="Contact · Love Your Health Natural Medicine" description="Get in touch with Love Your Health Natural Medicine in Tempe, AZ. Call (480) 914-0521, book online, or send us a message. In-person and virtual naturopathic care across Arizona.">

  <!-- ==================== 1. HERO ==================== -->
  <section class="relative overflow-hidden bg-[#1a2516]">
    <!-- Ambient glow -->
    <div class="absolute top-0 left-1/2 -translate-x-1/2 w-[50rem] h-[50rem] rounded-full bg-sage/8 blur-[160px] -translate-y-1/2 pointer-events-none" aria-hidden="true"></div>
    <div class="absolute bottom-0 right-0 w-[30rem] h-[30rem] rounded-full bg-terracotta/5 blur-[120px] translate-y-1/3 translate-x-1/4 pointer-events-none" aria-hidden="true"></div>

    <div class="relative z-10 section-padding">
      <div class="container-main">
        <div class="text-center max-w-3xl mx-auto">

          <!-- Overline pill -->
          <div class="hero-el flex justify-center mb-6">
            <span class="inline-flex items-center gap-2 bg-sage/15 rounded-full px-4 py-1.5 text-xs font-bold text-sage-light tracking-wide uppercase">
              <svg class="w-3.5 h-3.5" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
                <path d="M3 8l7.89 5.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z"/>
              </svg>
              Get in Touch
            </span>
          </div>

          <!-- H1 -->
          <h1 class="hero-el text-[clamp(2.4rem,6.5vw,4.5rem)] font-black leading-[1.04] tracking-[-0.03em] text-white mb-5">
            We'd love to <span class="text-sage-light">hear from you.</span>
          </h1>

          <!-- Subtitle -->
          <p class="hero-el text-base md:text-lg text-white/70 leading-relaxed max-w-xl mx-auto mb-10">
            Whether you're ready to book your first visit, have a question about our approach, or just need directions — we're here to help.
          </p>

          <!-- Quick-action pills -->
          <div class="hero-el flex flex-wrap justify-center gap-3">
            <a
              href="tel:+14809140521"
              class="magnetic-btn group inline-flex items-center gap-2.5 bg-sage/15 hover:bg-sage/25 border border-sage/20 text-white font-bold text-sm px-6 py-3.5 rounded-full transition-all duration-300"
            >
              <svg class="w-4 h-4 text-sage-light" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
                <path d="M22 16.92v3a2 2 0 01-2.18 2 19.79 19.79 0 01-8.63-3.07 19.5 19.5 0 01-6-6 19.79 19.79 0 01-3.07-8.67A2 2 0 014.11 2h3a2 2 0 012 1.72c.127.96.361 1.903.7 2.81a2 2 0 01-.45 2.11L8.09 9.91a16 16 0 006 6l1.27-1.27a2 2 0 012.11-.45c.907.339 1.85.573 2.81.7A2 2 0 0122 16.92z"/>
              </svg>
              (480) 914-0521
            </a>
            <a
              href="mailto:info@loveyourhealthnaturalmedicine.com"
              class="magnetic-btn group inline-flex items-center gap-2.5 bg-white/8 hover:bg-white/14 border border-white/12 text-white font-bold text-sm px-6 py-3.5 rounded-full transition-all duration-300"
            >
              <svg class="w-4 h-4 text-white/70" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
                <path d="M3 8l7.89 5.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z"/>
              </svg>
              Email Us
            </a>
            <a
              href="https://loveyourhealthnaturalmedicine.com"
              target="_blank"
              rel="noopener noreferrer"
              class="magnetic-btn cta-shine group relative inline-flex items-center gap-2.5 bg-sage hover:bg-sage-dark text-white font-bold text-sm px-6 py-3.5 rounded-full transition-all duration-300 hover:shadow-xl hover:shadow-sage/25 overflow-hidden"
            >
              <span class="relative z-10 flex items-center gap-2.5">
                <svg class="w-4 h-4" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
                  <rect x="3" y="4" width="18" height="18" rx="2"/><line x1="16" y1="2" x2="16" y2="6"/><line x1="8" y1="2" x2="8" y2="6"/><line x1="3" y1="10" x2="21" y2="10"/>
                </svg>
                Book Online
              </span>
            </a>
          </div>

        </div>
      </div>
    </div>
  </section>

  <!-- ==================== 2. CONTACT CARDS ==================== -->
  <section class="section-padding bg-white relative overflow-hidden">
    <div class="absolute top-0 left-1/2 -translate-x-1/2 w-[40rem] h-[40rem] rounded-full bg-sage/4 blur-[140px] -translate-y-1/2 pointer-events-none" aria-hidden="true"></div>

    <div class="container-main relative z-10">
      <div class="grid grid-cols-1 md:grid-cols-3 gap-6 max-w-5xl mx-auto">

        <!-- Card 1: Call Us -->
        <div class="scroll-reveal bg-white rounded-[1.5rem] border border-sage-pale/40 shadow-sm overflow-hidden group hover:shadow-md transition-shadow duration-300">
          <!-- Colored top bar -->
          <div class="h-1.5 bg-gradient-to-r from-sage to-sage-light"></div>
          <div class="p-7">
            <!-- Icon -->
            <div class="w-12 h-12 rounded-2xl bg-sage-pale/60 flex items-center justify-center mb-5">
              <svg class="w-5.5 h-5.5 text-sage" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
                <path d="M22 16.92v3a2 2 0 01-2.18 2 19.79 19.79 0 01-8.63-3.07 19.5 19.5 0 01-6-6 19.79 19.79 0 01-3.07-8.67A2 2 0 014.11 2h3a2 2 0 012 1.72c.127.96.361 1.903.7 2.81a2 2 0 01-.45 2.11L8.09 9.91a16 16 0 006 6l1.27-1.27a2 2 0 012.11-.45c.907.339 1.85.573 2.81.7A2 2 0 0122 16.92z"/>
              </svg>
            </div>
            <h3 class="text-lg font-bold text-charcoal mb-1.5">Call Us</h3>
            <a href="tel:+14809140521" class="text-sage font-bold text-base hover:text-sage-dark transition-colors">(480) 914-0521</a>
            <p class="text-sm text-text-light mt-2 leading-relaxed">Mon&ndash;Fri during office hours. We're happy to answer questions or help you get started.</p>
            <a href="tel:+14809140521" class="inline-flex items-center gap-1.5 text-sm font-bold text-sage hover:text-sage-dark mt-4 transition-colors group/link">
              Call now
              <svg class="w-3.5 h-3.5 transition-transform duration-300 group-hover/link:translate-x-1" fill="none" stroke="currentColor" stroke-width="2.5" viewBox="0 0 24 24"><path d="M5 12h14M12 5l7 7-7 7"/></svg>
            </a>
          </div>
        </div>

        <!-- Card 2: Book Online -->
        <div class="scroll-reveal bg-white rounded-[1.5rem] border border-sage-pale/40 shadow-sm overflow-hidden group hover:shadow-md transition-shadow duration-300" style="--reveal-delay: 0.1s;">
          <!-- Colored top bar -->
          <div class="h-1.5 bg-gradient-to-r from-terracotta to-terracotta-light"></div>
          <div class="p-7">
            <!-- Icon -->
            <div class="w-12 h-12 rounded-2xl bg-terracotta/10 flex items-center justify-center mb-5">
              <svg class="w-5.5 h-5.5 text-terracotta" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
                <rect x="3" y="4" width="18" height="18" rx="2"/><line x1="16" y1="2" x2="16" y2="6"/><line x1="8" y1="2" x2="8" y2="6"/><line x1="3" y1="10" x2="21" y2="10"/>
              </svg>
            </div>
            <h3 class="text-lg font-bold text-charcoal mb-1.5">Book Online</h3>
            <p class="text-sm text-text-light leading-relaxed">Schedule your visit in just a few clicks. New and returning patients welcome.</p>
            <a href="https://loveyourhealthnaturalmedicine.com" target="_blank" rel="noopener noreferrer" class="inline-flex items-center gap-1.5 text-sm font-bold text-terracotta hover:text-terracotta-light mt-4 transition-colors group/link">
              Schedule your visit
              <svg class="w-3.5 h-3.5 transition-transform duration-300 group-hover/link:translate-x-1" fill="none" stroke="currentColor" stroke-width="2.5" viewBox="0 0 24 24"><path d="M5 12h14M12 5l7 7-7 7"/></svg>
            </a>
          </div>
        </div>

        <!-- Card 3: Virtual Care -->
        <div class="scroll-reveal bg-white rounded-[1.5rem] border border-sage-pale/40 shadow-sm overflow-hidden group hover:shadow-md transition-shadow duration-300" style="--reveal-delay: 0.2s;">
          <!-- Colored top bar -->
          <div class="h-1.5 bg-gradient-to-r from-earth to-[#A68B6B]"></div>
          <div class="p-7">
            <!-- Icon -->
            <div class="w-12 h-12 rounded-2xl bg-earth/10 flex items-center justify-center mb-5">
              <svg class="w-5.5 h-5.5 text-earth" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
                <rect x="2" y="3" width="20" height="14" rx="2"/><line x1="8" y1="21" x2="16" y2="21"/><line x1="12" y1="17" x2="12" y2="21"/>
              </svg>
            </div>
            <h3 class="text-lg font-bold text-charcoal mb-1.5">Virtual Care</h3>
            <p class="text-sm text-text-light leading-relaxed">Available across all of Arizona. Same thorough visits from the comfort of home.</p>
            <a href="/virtual-care" class="inline-flex items-center gap-1.5 text-sm font-bold text-earth hover:text-[#6B5A42] mt-4 transition-colors group/link">
              Learn about virtual visits
              <svg class="w-3.5 h-3.5 transition-transform duration-300 group-hover/link:translate-x-1" fill="none" stroke="currentColor" stroke-width="2.5" viewBox="0 0 24 24"><path d="M5 12h14M12 5l7 7-7 7"/></svg>
            </a>
          </div>
        </div>

      </div>
    </div>
  </section>

  <!-- ==================== 3. LOCATION & HOURS ==================== -->
  <section class="section-padding bg-warm-white relative overflow-hidden">
    <div class="absolute bottom-0 right-0 w-[35rem] h-[35rem] rounded-full bg-sage/4 blur-[140px] translate-y-1/3 translate-x-1/4 pointer-events-none" aria-hidden="true"></div>

    <div class="container-main relative z-10">

      <!-- Section heading -->
      <div class="text-center max-w-2xl mx-auto mb-12 scroll-reveal">
        <p class="text-xs md:text-sm font-medium uppercase tracking-[0.25em] text-terracotta mb-4">Visit Us</p>
        <h2 class="text-[clamp(1.8rem,4.5vw,3.2rem)] font-black leading-[1.1] tracking-[-0.02em] text-charcoal">
          Where to <span class="text-sage">find us.</span>
        </h2>
      </div>

      <div class="grid grid-cols-1 md:grid-cols-2 gap-6 max-w-5xl mx-auto">

        <!-- Left: Find Us -->
        <div class="scroll-reveal bg-white rounded-[1.5rem] p-7 border border-sage-pale/40 shadow-sm">
          <div class="flex items-center gap-3 mb-6">
            <div class="w-10 h-10 rounded-xl bg-sage-pale/60 flex items-center justify-center">
              <svg class="w-5 h-5 text-sage" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
                <path d="M21 10c0 7-9 13-9 13s-9-6-9-13a9 9 0 0118 0z"/><circle cx="12" cy="10" r="3"/>
              </svg>
            </div>
            <h3 class="text-xl font-bold text-charcoal">Find Us</h3>
          </div>

          <div class="space-y-4">
            <!-- Address -->
            <div>
              <p class="text-base font-semibold text-charcoal leading-snug">1030 E Baseline Rd, Suite 108</p>
              <p class="text-base font-semibold text-charcoal">Tempe, AZ 85283</p>
            </div>

            <!-- Directions note -->
            <div class="bg-sage-pale/30 rounded-xl px-4 py-3">
              <p class="text-sm text-text-light leading-relaxed">
                <span class="font-semibold text-charcoal">Near the Baseline &amp; Rural intersection.</span><br/>
                Free parking on-site.
              </p>
            </div>

            <!-- Map link -->
            <a
              href="https://www.google.com/maps/dir/?api=1&destination=1030+E+Baseline+Rd+Suite+108+Tempe+AZ+85283"
              target="_blank"
              rel="noopener noreferrer"
              class="magnetic-btn inline-flex items-center gap-2 bg-sage-pale/50 hover:bg-sage-pale text-sage font-bold text-sm px-5 py-3 rounded-full transition-all duration-300"
            >
              <svg class="w-4 h-4" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
                <path d="M9 20l-5.447-2.724A1 1 0 013 16.382V5.618a1 1 0 011.447-.894L9 7m0 13l6-3m-6 3V7m6 10l5.447 2.724A1 1 0 0021 18.382V7.618a1 1 0 00-.553-.894L15 4m0 13V4m0 0L9 7"/>
              </svg>
              Get Directions
            </a>
          </div>
        </div>

        <!-- Right: Office Hours -->
        <div class="scroll-reveal bg-white rounded-[1.5rem] p-7 border border-sage-pale/40 shadow-sm" style="--reveal-delay: 0.1s;">
          <div class="flex items-center gap-3 mb-6">
            <div class="w-10 h-10 rounded-xl bg-terracotta/10 flex items-center justify-center">
              <svg class="w-5 h-5 text-terracotta" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
                <circle cx="12" cy="12" r="10"/><polyline points="12 6 12 12 16 14"/>
              </svg>
            </div>
            <h3 class="text-xl font-bold text-charcoal">Office Hours</h3>
          </div>

          <!-- Hours table -->
          <div class="space-y-0">
            <div class="flex justify-between items-center py-3 border-b border-sage-pale/40">
              <span class="text-sm font-semibold text-charcoal">Monday</span>
              <span class="text-sm text-text-light">9:00 AM &ndash; 6:00 PM</span>
            </div>
            <div class="flex justify-between items-center py-3 border-b border-sage-pale/40">
              <span class="text-sm font-semibold text-charcoal">Tuesday</span>
              <span class="text-sm text-text-light">8:00 AM &ndash; 5:00 PM</span>
            </div>
            <div class="flex justify-between items-center py-3 border-b border-sage-pale/40">
              <span class="text-sm font-semibold text-charcoal">Wednesday</span>
              <span class="text-sm text-text-light">8:00 AM &ndash; 5:00 PM</span>
            </div>
            <div class="flex justify-between items-center py-3 border-b border-sage-pale/40">
              <span class="text-sm font-semibold text-charcoal">Thursday</span>
              <span class="text-sm text-text-light">8:00 AM &ndash; 5:00 PM</span>
            </div>
            <div class="flex justify-between items-center py-3 border-b border-sage-pale/40">
              <span class="text-sm font-semibold text-charcoal">Friday</span>
              <span class="text-sm text-text-light">9:00 AM &ndash; 2:00 PM</span>
            </div>
            <div class="flex justify-between items-center py-3">
              <span class="text-sm font-semibold text-charcoal">Saturday &ndash; Sunday</span>
              <span class="text-sm text-text-light/60 font-medium">Closed</span>
            </div>
          </div>

          <!-- Virtual note -->
          <div class="mt-5 bg-earth/8 rounded-xl px-4 py-3 flex items-start gap-2.5">
            <svg class="w-4 h-4 text-earth shrink-0 mt-0.5" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
              <rect x="2" y="3" width="20" height="14" rx="2"/><line x1="8" y1="21" x2="16" y2="21"/><line x1="12" y1="17" x2="12" y2="21"/>
            </svg>
            <p class="text-xs text-text-light leading-relaxed">
              <span class="font-semibold text-charcoal">Virtual care available</span> across all of Arizona during office hours.
            </p>
          </div>
        </div>

      </div>
    </div>
  </section>

  <!-- ==================== 4. CONTACT FORM ==================== -->
  <section class="section-padding bg-white relative overflow-hidden">
    <div class="absolute top-1/2 left-0 -translate-y-1/2 w-[30rem] h-[30rem] rounded-full bg-sage/3 blur-[140px] -translate-x-1/2 pointer-events-none" aria-hidden="true"></div>

    <div class="container-main relative z-10">
      <div class="max-w-2xl mx-auto">

        <!-- Heading -->
        <div class="text-center mb-10 scroll-reveal">
          <p class="text-xs md:text-sm font-medium uppercase tracking-[0.25em] text-sage mb-4">Message Us</p>
          <h2 class="text-[clamp(1.8rem,4.5vw,3.2rem)] font-black leading-[1.1] tracking-[-0.02em] text-charcoal mb-3">
            Send us a <span class="text-sage">message.</span>
          </h2>
          <p class="text-sm md:text-base text-text-light leading-relaxed max-w-md mx-auto">
            Have a question before booking? Drop us a note and we'll get back to you.
          </p>
        </div>

        <!-- Form -->
        <form
          name="contact"
          method="POST"
          data-netlify="true"
          class="scroll-reveal space-y-5"
          id="contact-form"
        >
          <input type="hidden" name="form-name" value="contact" />

          <!-- Name + Email row -->
          <div class="grid grid-cols-1 md:grid-cols-2 gap-5">
            <div>
              <label for="name" class="block text-sm font-semibold text-charcoal mb-2">Name <span class="text-terracotta">*</span></label>
              <input
                type="text"
                id="name"
                name="name"
                required
                autocomplete="name"
                placeholder="Your full name"
                class="w-full px-4 py-3 bg-warm-white border border-sage-pale/60 rounded-xl text-sm text-charcoal placeholder:text-text-light/50 focus:outline-none focus:ring-2 focus:ring-sage/30 focus:border-sage/40 transition-all duration-200"
              />
            </div>
            <div>
              <label for="email" class="block text-sm font-semibold text-charcoal mb-2">Email <span class="text-terracotta">*</span></label>
              <input
                type="email"
                id="email"
                name="email"
                required
                autocomplete="email"
                placeholder="you@example.com"
                class="w-full px-4 py-3 bg-warm-white border border-sage-pale/60 rounded-xl text-sm text-charcoal placeholder:text-text-light/50 focus:outline-none focus:ring-2 focus:ring-sage/30 focus:border-sage/40 transition-all duration-200"
              />
            </div>
          </div>

          <!-- Phone + Subject row -->
          <div class="grid grid-cols-1 md:grid-cols-2 gap-5">
            <div>
              <label for="phone" class="block text-sm font-semibold text-charcoal mb-2">Phone <span class="text-text-light text-xs font-normal">(optional)</span></label>
              <input
                type="tel"
                id="phone"
                name="phone"
                autocomplete="tel"
                placeholder="(480) 000-0000"
                class="w-full px-4 py-3 bg-warm-white border border-sage-pale/60 rounded-xl text-sm text-charcoal placeholder:text-text-light/50 focus:outline-none focus:ring-2 focus:ring-sage/30 focus:border-sage/40 transition-all duration-200"
              />
            </div>
            <div>
              <label for="subject" class="block text-sm font-semibold text-charcoal mb-2">Subject <span class="text-terracotta">*</span></label>
              <div class="relative">
                <select
                  id="subject"
                  name="subject"
                  required
                  class="w-full px-4 py-3 bg-warm-white border border-sage-pale/60 rounded-xl text-sm text-charcoal focus:outline-none focus:ring-2 focus:ring-sage/30 focus:border-sage/40 transition-all duration-200 appearance-none cursor-pointer"
                >
                  <option value="" disabled selected>Select a topic</option>
                  <option value="General Inquiry">General Inquiry</option>
                  <option value="New Patient Question">New Patient Question</option>
                  <option value="Existing Patient">Existing Patient</option>
                  <option value="Insurance Question">Insurance Question</option>
                  <option value="Other">Other</option>
                </select>
                <svg class="absolute right-3.5 top-1/2 -translate-y-1/2 w-4 h-4 text-text-light pointer-events-none" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
                  <polyline points="6 9 12 15 18 9"/>
                </svg>
              </div>
            </div>
          </div>

          <!-- Message -->
          <div>
            <label for="message" class="block text-sm font-semibold text-charcoal mb-2">Message <span class="text-terracotta">*</span></label>
            <textarea
              id="message"
              name="message"
              required
              rows="5"
              placeholder="How can we help you?"
              class="w-full px-4 py-3 bg-warm-white border border-sage-pale/60 rounded-xl text-sm text-charcoal placeholder:text-text-light/50 focus:outline-none focus:ring-2 focus:ring-sage/30 focus:border-sage/40 transition-all duration-200 resize-vertical min-h-[120px]"
            ></textarea>
          </div>

          <!-- Submit -->
          <div class="flex flex-col items-center gap-4 pt-2">
            <button
              type="submit"
              class="magnetic-btn cta-shine group relative inline-flex items-center justify-center gap-2 bg-sage hover:bg-sage-dark text-white font-bold text-sm px-8 py-4 rounded-full transition-all duration-300 hover:shadow-xl hover:shadow-sage/25 overflow-hidden w-full md:w-auto"
            >
              <span class="relative z-10 flex items-center gap-2">
                Send Message
                <svg class="w-3.5 h-3.5 transition-transform duration-300 group-hover:translate-x-1" fill="none" stroke="currentColor" stroke-width="2.5" viewBox="0 0 24 24"><path d="M5 12h14M12 5l7 7-7 7"/></svg>
              </span>
            </button>
            <p class="text-xs text-text-light flex items-center gap-1.5">
              <svg class="w-3.5 h-3.5 text-sage" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
                <circle cx="12" cy="12" r="10"/><polyline points="12 6 12 12 16 14"/>
              </svg>
              We typically respond within 1 business day.
            </p>
          </div>
        </form>

        <!-- Success message (hidden by default) -->
        <div id="form-success" class="hidden text-center py-12">
          <div class="w-16 h-16 rounded-full bg-sage-pale/60 flex items-center justify-center mx-auto mb-5">
            <svg class="w-8 h-8 text-sage" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
              <polyline points="20 6 9 17 4 12"/>
            </svg>
          </div>
          <h3 class="text-xl font-bold text-charcoal mb-2">Message sent!</h3>
          <p class="text-sm text-text-light">Thank you for reaching out. We'll get back to you within 1 business day.</p>
        </div>

      </div>
    </div>
  </section>

  <!-- ==================== 5. QUICK LINKS ==================== -->
  <section class="section-padding bg-cream relative overflow-hidden">
    <div class="absolute top-0 right-0 w-[35rem] h-[35rem] rounded-full bg-terracotta/4 blur-[140px] -translate-y-1/3 translate-x-1/4 pointer-events-none" aria-hidden="true"></div>

    <div class="container-main relative z-10">

      <!-- Heading -->
      <div class="text-center max-w-2xl mx-auto mb-12 scroll-reveal">
        <p class="text-xs md:text-sm font-medium uppercase tracking-[0.25em] text-earth mb-4">Explore</p>
        <h2 class="text-[clamp(1.8rem,4.5vw,3.2rem)] font-black leading-[1.1] tracking-[-0.02em] text-charcoal">
          Looking for something <span class="text-sage">specific?</span>
        </h2>
      </div>

      <!-- Link cards grid -->
      <div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-4 gap-5 max-w-5xl mx-auto">

        <!-- New Patients -->
        <a href="/new-patients" class="scroll-reveal group bg-white rounded-[1.5rem] p-6 border border-sage-pale/40 shadow-sm hover:shadow-md hover:border-sage/20 transition-all duration-300">
          <div class="w-11 h-11 rounded-xl bg-sage-pale/60 flex items-center justify-center mb-4 group-hover:bg-sage-pale transition-colors duration-300">
            <svg class="w-5 h-5 text-sage" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
              <path d="M16 21v-2a4 4 0 00-4-4H5a4 4 0 00-4-4v2"/><circle cx="8.5" cy="7" r="4"/><path d="M20 8v6M23 11h-6"/>
            </svg>
          </div>
          <h3 class="text-sm font-bold text-charcoal mb-1 group-hover:text-sage transition-colors duration-300">New Patients</h3>
          <p class="text-xs text-text-light leading-relaxed mb-3">Everything you need to know for your first visit.</p>
          <span class="inline-flex items-center gap-1 text-xs font-bold text-sage">
            Learn more
            <svg class="w-3 h-3 transition-transform duration-300 group-hover:translate-x-1" fill="none" stroke="currentColor" stroke-width="2.5" viewBox="0 0 24 24"><path d="M5 12h14M12 5l7 7-7 7"/></svg>
          </span>
        </a>

        <!-- Pricing -->
        <a href="/pricing" class="scroll-reveal group bg-white rounded-[1.5rem] p-6 border border-sage-pale/40 shadow-sm hover:shadow-md hover:border-terracotta/20 transition-all duration-300" style="--reveal-delay: 0.1s;">
          <div class="w-11 h-11 rounded-xl bg-terracotta/10 flex items-center justify-center mb-4 group-hover:bg-terracotta/15 transition-colors duration-300">
            <svg class="w-5 h-5 text-terracotta" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
              <line x1="12" y1="1" x2="12" y2="23"/><path d="M17 5H9.5a3.5 3.5 0 000 7h5a3.5 3.5 0 010 7H6"/>
            </svg>
          </div>
          <h3 class="text-sm font-bold text-charcoal mb-1 group-hover:text-terracotta transition-colors duration-300">Pricing</h3>
          <p class="text-xs text-text-light leading-relaxed mb-3">Transparent costs, memberships, and payment info.</p>
          <span class="inline-flex items-center gap-1 text-xs font-bold text-terracotta">
            View pricing
            <svg class="w-3 h-3 transition-transform duration-300 group-hover:translate-x-1" fill="none" stroke="currentColor" stroke-width="2.5" viewBox="0 0 24 24"><path d="M5 12h14M12 5l7 7-7 7"/></svg>
          </span>
        </a>

        <!-- FAQs -->
        <a href="/faqs" class="scroll-reveal group bg-white rounded-[1.5rem] p-6 border border-sage-pale/40 shadow-sm hover:shadow-md hover:border-earth/20 transition-all duration-300" style="--reveal-delay: 0.2s;">
          <div class="w-11 h-11 rounded-xl bg-earth/10 flex items-center justify-center mb-4 group-hover:bg-earth/15 transition-colors duration-300">
            <svg class="w-5 h-5 text-earth" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
              <circle cx="12" cy="12" r="10"/><path d="M9.09 9a3 3 0 015.83 1c0 2-3 3-3 3"/><line x1="12" y1="17" x2="12.01" y2="17"/>
            </svg>
          </div>
          <h3 class="text-sm font-bold text-charcoal mb-1 group-hover:text-earth transition-colors duration-300">FAQs</h3>
          <p class="text-xs text-text-light leading-relaxed mb-3">Answers to common questions about our practice.</p>
          <span class="inline-flex items-center gap-1 text-xs font-bold text-earth">
            Read FAQs
            <svg class="w-3 h-3 transition-transform duration-300 group-hover:translate-x-1" fill="none" stroke="currentColor" stroke-width="2.5" viewBox="0 0 24 24"><path d="M5 12h14M12 5l7 7-7 7"/></svg>
          </span>
        </a>

        <!-- Our Doctors -->
        <a href="/doctors" class="scroll-reveal group bg-white rounded-[1.5rem] p-6 border border-sage-pale/40 shadow-sm hover:shadow-md hover:border-sage/20 transition-all duration-300" style="--reveal-delay: 0.3s;">
          <div class="w-11 h-11 rounded-xl bg-sage-pale/60 flex items-center justify-center mb-4 group-hover:bg-sage-pale transition-colors duration-300">
            <svg class="w-5 h-5 text-sage" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
              <path d="M17 21v-2a4 4 0 00-4-4H5a4 4 0 00-4-4v2"/><circle cx="9" cy="7" r="4"/><path d="M23 21v-2a4 4 0 00-3-3.87"/><path d="M16 3.13a4 4 0 010 7.75"/>
            </svg>
          </div>
          <h3 class="text-sm font-bold text-charcoal mb-1 group-hover:text-sage transition-colors duration-300">Our Doctors</h3>
          <p class="text-xs text-text-light leading-relaxed mb-3">Meet Dr. Warren, Dr. Perrino, and Dr. Newman.</p>
          <span class="inline-flex items-center gap-1 text-xs font-bold text-sage">
            Meet the team
            <svg class="w-3 h-3 transition-transform duration-300 group-hover:translate-x-1" fill="none" stroke="currentColor" stroke-width="2.5" viewBox="0 0 24 24"><path d="M5 12h14M12 5l7 7-7 7"/></svg>
          </span>
        </a>

      </div>
    </div>
  </section>

  <!-- ==================== 6. CTA ==================== -->
  <section class="relative overflow-hidden bg-[#1a2516]">
    <!-- Ambient glow -->
    <div class="absolute top-0 left-1/2 -translate-x-1/2 w-[40rem] h-[40rem] rounded-full bg-sage/8 blur-[140px] -translate-y-1/3 pointer-events-none" aria-hidden="true"></div>
    <div class="absolute bottom-0 right-0 w-[25rem] h-[25rem] rounded-full bg-terracotta/5 blur-[100px] translate-y-1/3 translate-x-1/4 pointer-events-none" aria-hidden="true"></div>

    <div class="relative z-10 section-padding">
      <div class="container-main">
        <div class="text-center max-w-3xl mx-auto scroll-reveal">

          <!-- Leaf icon -->
          <div class="flex justify-center mb-6">
            <div class="w-12 h-12 rounded-2xl bg-sage/15 flex items-center justify-center">
              <svg class="w-6 h-6 text-sage-light" fill="none" stroke="currentColor" stroke-width="1.5" viewBox="0 0 24 24">
                <path d="M12 22c-4-4-8-7.5-8-12a8 8 0 0116 0c0 4.5-4 8-8 12z"/><path d="M12 22V10"/><path d="M8 14c1.5-1 3-1.5 4-4"/><path d="M16 14c-1.5-1-3-1.5-4-4"/>
              </svg>
            </div>
          </div>

          <h2 class="text-[clamp(2rem,5vw,3.5rem)] font-black leading-[1.08] tracking-[-0.03em] text-white mb-4">
            Ready to start your <span class="text-sage-light">health journey?</span>
          </h2>
          <p class="text-base md:text-lg text-white/60 leading-relaxed max-w-lg mx-auto mb-10">
            Three naturopathic doctors, one team. Real answers — not band-aids. Your first step starts here.
          </p>

          <div class="flex flex-wrap justify-center gap-4">
            <a
              href="https://loveyourhealthnaturalmedicine.com"
              target="_blank"
              rel="noopener noreferrer"
              class="magnetic-btn cta-shine group relative inline-flex items-center justify-center gap-2 bg-sage hover:bg-sage-dark text-white font-bold text-sm px-8 py-4 rounded-full transition-all duration-300 hover:shadow-xl hover:shadow-sage/25 overflow-hidden"
            >
              <span class="relative z-10 flex items-center gap-2">
                Book a Consultation
                <svg class="w-3.5 h-3.5 transition-transform duration-300 group-hover:translate-x-1" fill="none" stroke="currentColor" stroke-width="2.5" viewBox="0 0 24 24"><path d="M5 12h14M12 5l7 7-7 7"/></svg>
              </span>
            </a>
            <a
              href="tel:+14809140521"
              class="magnetic-btn relative inline-flex items-center justify-center gap-2 bg-transparent hover:bg-white/8 text-white font-bold text-sm px-8 py-4 rounded-full transition-all duration-300 border border-white/20 hover:border-white/35 overflow-hidden"
            >
              <svg class="w-4 h-4 text-white/70" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
                <path d="M22 16.92v3a2 2 0 01-2.18 2 19.79 19.79 0 01-8.63-3.07 19.5 19.5 0 01-6-6 19.79 19.79 0 01-3.07-8.67A2 2 0 014.11 2h3a2 2 0 012 1.72c.127.96.361 1.903.7 2.81a2 2 0 01-.45 2.11L8.09 9.91a16 16 0 006 6l1.27-1.27a2 2 0 012.11-.45c.907.339 1.85.573 2.81.7A2 2 0 0122 16.92z"/>
              </svg>
              Call (480) 914-0521
            </a>
          </div>

        </div>
      </div>
    </div>
  </section>

</BaseLayout>

<!-- ==================== STYLES ==================== -->
<style>
  /* Hero stagger animation */
  .hero-el {
    opacity: 0;
    transform: translateY(24px);
    animation: heroFadeUp 0.7s ease-out forwards;
  }
  .hero-el:nth-child(1) { animation-delay: 0.1s; }
  .hero-el:nth-child(2) { animation-delay: 0.22s; }
  .hero-el:nth-child(3) { animation-delay: 0.34s; }
  .hero-el:nth-child(4) { animation-delay: 0.46s; }
  .hero-el:nth-child(5) { animation-delay: 0.58s; }

  @keyframes heroFadeUp {
    from {
      opacity: 0;
      transform: translateY(24px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  /* Scroll reveal */
  .scroll-reveal {
    opacity: 0;
    transform: translateY(28px);
    transition: opacity 0.7s ease-out, transform 0.7s ease-out;
    transition-delay: var(--reveal-delay, 0s);
  }
  .scroll-reveal.revealed {
    opacity: 1;
    transform: translateY(0);
  }

  /* CTA shine sweep */
  .cta-shine::before {
    content: '';
    position: absolute;
    top: 0;
    left: -100%;
    width: 100%;
    height: 100%;
    background: linear-gradient(
      90deg,
      transparent 0%,
      rgba(255, 255, 255, 0.15) 50%,
      transparent 100%
    );
    transition: left 0.6s ease;
    z-index: 1;
  }
  .cta-shine:hover::before {
    left: 100%;
  }

  /* Magnetic button base */
  .magnetic-btn {
    transition: transform 0.25s ease-out, box-shadow 0.3s ease, background-color 0.3s ease, border-color 0.3s ease;
    will-change: transform;
  }

  /* Form focus enhancements */
  input:focus,
  select:focus,
  textarea:focus {
    box-shadow: 0 0 0 3px rgba(91, 140, 111, 0.1);
  }

  /* Select dropdown custom styling */
  select option {
    padding: 8px 12px;
    background: #FFFDF9;
    color: #2D2D2D;
  }

  /* Reduced motion: disable ALL animations */
  @media (prefers-reduced-motion: reduce) {
    .hero-el {
      opacity: 1;
      transform: none;
      animation: none;
    }
    .scroll-reveal {
      opacity: 1;
      transform: none;
      transition: none;
    }
    .cta-shine::before {
      display: none;
    }
    .magnetic-btn {
      transition: background-color 0.15s ease, border-color 0.15s ease;
      transform: none !important;
    }
    * {
      animation-duration: 0.01ms !important;
      animation-iteration-count: 1 !important;
      transition-duration: 0.01ms !important;
      scroll-behavior: auto !important;
    }
  }
</style>

<!-- ==================== SCRIPTS ==================== -->
<script>
  // ---- Scroll Reveal (IntersectionObserver) ----
  function initScrollReveal() {
    const revealElements = document.querySelectorAll('.scroll-reveal');
    if (!revealElements.length) return;

    // Respect reduced motion
    const prefersReducedMotion = window.matchMedia('(prefers-reduced-motion: reduce)').matches;
    if (prefersReducedMotion) {
      revealElements.forEach((el) => el.classList.add('revealed'));
      return;
    }

    const observer = new IntersectionObserver(
      (entries) => {
        entries.forEach((entry) => {
          if (entry.isIntersecting) {
            entry.target.classList.add('revealed');
            observer.unobserve(entry.target);
          }
        });
      },
      {
        threshold: 0.12,
        rootMargin: '0px 0px -40px 0px',
      }
    );

    revealElements.forEach((el) => observer.observe(el));
  }

  // ---- Magnetic Buttons ----
  function initMagneticButtons() {
    const prefersReducedMotion = window.matchMedia('(prefers-reduced-motion: reduce)').matches;
    if (prefersReducedMotion) return;

    const magneticBtns = document.querySelectorAll('.magnetic-btn');

    magneticBtns.forEach((btn) => {
      btn.addEventListener('mousemove', (e) => {
        const rect = btn.getBoundingClientRect();
        const x = e.clientX - rect.left - rect.width / 2;
        const y = e.clientY - rect.top - rect.height / 2;
        const strength = 0.15;
        btn.style.transform = `translate(${x * strength}px, ${y * strength}px)`;
      });

      btn.addEventListener('mouseleave', () => {
        btn.style.transform = 'translate(0px, 0px)';
      });
    });
  }

  // ---- Form handling ----
  function initContactForm() {
    const form = document.getElementById('contact-form');
    const successMsg = document.getElementById('form-success');
    if (!form || !successMsg) return;

    form.addEventListener('submit', async (e) => {
      e.preventDefault();

      const formData = new FormData(form);
      const submitBtn = form.querySelector('button[type="submit"]');
      const originalContent = submitBtn.innerHTML;

      // Show loading state
      submitBtn.disabled = true;
      submitBtn.innerHTML = `
        <span class="relative z-10 flex items-center gap-2">
          Sending...
          <svg class="w-3.5 h-3.5 animate-spin" fill="none" viewBox="0 0 24 24">
            <circle class="opacity-25" cx="12" cy="12" r="10" stroke="currentColor" stroke-width="4"></circle>
            <path class="opacity-75" fill="currentColor" d="M4 12a8 8 0 018-8V0C5.373 0 0 5.373 0 12h4zm2 5.291A7.962 7.962 0 014 12H0c0 3.042 1.135 5.824 3 7.938l3-2.647z"></path>
          </svg>
        </span>
      `;

      try {
        const response = await fetch('/', {
          method: 'POST',
          headers: { 'Content-Type': 'application/x-www-form-urlencoded' },
          body: new URLSearchParams(formData).toString(),
        });

        if (response.ok) {
          form.classList.add('hidden');
          successMsg.classList.remove('hidden');
        } else {
          throw new Error('Form submission failed');
        }
      } catch (error) {
        // Restore button
        submitBtn.disabled = false;
        submitBtn.innerHTML = originalContent;
        alert('Something went wrong. Please try again or call us at (480) 914-0521.');
      }
    });
  }

  // ---- Init ----
  document.addEventListener('DOMContentLoaded', () => {
    initScrollReveal();
    initMagneticButtons();
    initContactForm();
  });

  // Also run on Astro page transitions
  document.addEventListener('astro:page-load', () => {
    initScrollReveal();
    initMagneticButtons();
    initContactForm();
  });
</script>
